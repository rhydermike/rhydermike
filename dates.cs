using System;
namespace Dates
{
    class Predict
    {
    static void Main ()
        {
            DateTime StartTime = new DateTime();
            Console.WriteLine(StartTime.ToString());
        }
    }
}